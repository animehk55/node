/**
 * Bind window event once
 * @function once
 * @param {string} event - Event to bind
 * @param {function} handler - Event handler
 */
'use strict'

const { once } = require('bwindow')

module.exports = once
