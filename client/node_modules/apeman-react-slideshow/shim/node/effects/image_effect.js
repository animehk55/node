/**
 * @function imageEffect
 */
'use strict';

var randomval = require('randomval');

/** @lends imageEffect */
function imageEffect(name, params) {
  var effect = imageEffect[name];
  if (!effect) {
    throw new Error('Unknown effect: ' + name);
  }
  return effect(params);
}

Object.assign(imageEffect, {
  fadeAndScale: function fadeAndScale(params) {
    var duration = params.duration || 500;
    var x = 50 + randomval.randomInt(-33, 33);
    var y = 50 + randomval.randomInt(-33, 33);
    var scale = 0.8;

    return {
      transition: 'opacity ' + duration / 5 + 'ms, transform ' + duration + 'ms',
      transformOrigin: x + '% ' + y + '%',
      transform: 'scale(' + scale + ', ' + scale + ')'
    };
  }
});

module.exports = imageEffect;
//# sourceMappingURL=data:application/json;base64,bnVsbA==